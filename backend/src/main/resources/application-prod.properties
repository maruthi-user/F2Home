# ============================================================
# PROD PROFILE
# ------------------------------------------------------------
# TODO: no production credentials exist yet - this currently
# mirrors application-dev.properties as a placeholder. Replace
# every value below (URLs, DB, JWT secret) with real production
# values once they are issued.
# ============================================================

app.url=https://api.f2home.com
frontend.url=https://f2home.com

#Postgres
# Real values (URL, username, password) come from a local, git-ignored .env
# file on the server - see .env.example. Never commit real credentials here.
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# F2HOME JWT signing secret (Base64, 256-bit) - also supplied via .env, never
# committed. MUST differ from dev's value.
f2home.jwt.secret-key=${F2HOME_JWT_SECRET}
